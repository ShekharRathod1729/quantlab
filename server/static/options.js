let comparisonChart;
loadStocks();
const formValues = () => ({ticker:document.querySelector('#stock-search').value, strike:document.querySelector('#strike').value, horizon:document.querySelector('#horizon').value, optionType:document.querySelector('#option-type').value, numSim:document.querySelector('#num-sim').value});
document.querySelector('#option-form').addEventListener('submit', async event => { event.preventDefault(); const button=event.submitter; button.disabled=true; message('Calculating BSM and Monte Carlo prices…','pending'); try { const data=await post('/api/options/mcs',formValues()); display(data); message('Pricing complete.'); } catch(error) { message(error.message,'error'); } finally { button.disabled=false; } });
document.querySelector('#bsm').addEventListener('click', async event => { event.currentTarget.disabled=true; message('Calculating the Black–Scholes–Merton price…','pending'); try { const data=await post('/api/options/bsm',formValues()); displayBsm(data); message('BSM pricing complete.'); } catch(error) { message(error.message,'error'); } finally { event.currentTarget.disabled=false; } });
document.querySelector('#compare').addEventListener('click', async event => { event.currentTarget.disabled=true; message('Building the convergence plot; this runs the full simulation range…','pending'); try { const data=await post('/api/options/comparison',formValues()); comparison(data); message('Convergence plot complete.'); } catch(error) { message(error.message,'error'); } finally { event.currentTarget.disabled=false; } });
function display(data) { const money=v=>`$${v.toFixed(4)}`; document.querySelector('#prices').innerHTML=stat('Black–Scholes–Merton',money(data.bsm.price))+stat(`Monte Carlo · ${data.simulations.toLocaleString()}`,money(data.monteCarlo))+stat('Difference',money(data.monteCarlo-data.bsm.price)); document.querySelector('#greek-values').innerHTML=['delta','gamma','theta','vega','rho'].map(key=>stat(key[0].toUpperCase()+key.slice(1),data.bsm[key].toFixed(6))).join(''); document.querySelector('#results').hidden=false; }
function displayBsm(data) { const money=v=>`$${v.toFixed(4)}`; document.querySelector('#prices').innerHTML=stat('Black–Scholes–Merton',money(data.result.price)); document.querySelector('#greek-values').innerHTML=['delta','gamma','theta','vega','rho'].map(key=>stat(key[0].toUpperCase()+key.slice(1),data.result[key].toFixed(6))).join(''); document.querySelector('#results').hidden=false; }
function comparison(data) { document.querySelector('#comparison-card').hidden=false; comparisonChart?.destroy(); comparisonChart=new Chart(document.querySelector('#comparison-chart'),{type:'line',data:{labels:data.simulations.map(x=>x.toLocaleString()),datasets:[{label:'Monte Carlo',data:data.prices,borderColor:'#56d4bb',tension:.2},{label:'BSM benchmark',data:data.simulations.map(()=>data.bsm.price),borderColor:'#75a7ff',borderDash:[7,5],pointRadius:0}]},options:{scales:{x:{title:{display:true,text:'Number of simulations'}},y:{title:{display:true,text:'Option price (USD)'}}}}}); }
